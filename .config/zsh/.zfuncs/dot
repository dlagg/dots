  seleccion=$(echo -e '/home/dlag/.config/zsh/.zshrc''\n''/home/dlag/.config/i3/config''\n''/home/dlag/.config/termite/config' | dmenu -nf red -sb green -sf red -fn 12)

  if [[ -n $seleccion ]]; then 
    nvim $(echo $seleccion)
  fi
  

